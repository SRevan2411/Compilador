#ifndef _GENERADORCODIGO
#define _GENERADORCODIGO

#include <iostream>
#include "conio.h"

using namespace std;

class Generador{
private:

public:
    Generador();
    int add(int,int);
    int sub(int,int);
    int mul(int,int);
    int divl(int,int);
    long double LD_SIN(long double);
    long double LD_COS(long double);
    long double LD_TAN(long double);





};

#endif // _GENERADORCODIGO
